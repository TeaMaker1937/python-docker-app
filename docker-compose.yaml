services:
  flask:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: flask-app
    ports:
      - "5000:5000"
    restart: always
    environment:
      - FLASK_ENV=production
